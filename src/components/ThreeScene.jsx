
import React from "react";
// ThreeScene removed per user request. This component keeps the layout
// slot by rendering an empty card container (no WebGL/Three.js).
export default function ThreeScene() {
  return (
    <div
      className="chart-card card"
      style={{ width: "100%", height: "100%" }}
    />
  );
}


